#!/usr/bin/python3

"""Uses PyDictionary to define words"""

import argparse
from PyDictionary import PyDictionary
dictionary = PyDictionary()

parser = argparse.ArgumentParser()
parser.add_argument('words', nargs='+')
parser.add_argument('--synonyms', '-s', action='store_true')
parser.add_argument('--antonyms', '-a', action='store_true')
parser.add_argument('--translate', '-t', default='')
args = parser.parse_args()

class color:
   PURPLE = '\033[95m'
   CYAN = '\033[96m'
   DARKCYAN = '\033[36m'
   BLUE = '\033[94m'
   GREEN = '\033[92m'
   YELLOW = '\033[93m'
   RED = '\033[91m'
   BOLD = '\033[1m'
   UNDERLINE = '\033[4m'
   END = '\033[0m'

for i in args.words:
    d = dictionary.meaning(i)
    print(color.BOLD + i + color.END)
    if d is not None:
        for k in d:
            print(color.GREEN + k + color.END)
            print("\n".join(d[k]))
        print("")
    else:
        print("No definitions found")
    if args.synonyms:
        print("Synonyms: " + ", ".join(dictionary.synonym(i)))
    if args.antonyms:
        print("Antonyms: " + ", ".join(dictionary.antonym(i)))
    if args.translate:
        print("Translation to %s: %s" % (args.translate, dictionary.translate(i, args.translate)))
